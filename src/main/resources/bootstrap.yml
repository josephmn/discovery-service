#server:
#  port: 8890
spring:
  application:
    name: discovery-service
    version: "@project.version@"
  profiles:
    active: prod
  cloud:
    config:
      uri: http://localhost:8888
  output:
    ansi:
      enabled: ALWAYS