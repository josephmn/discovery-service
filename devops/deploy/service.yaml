apiVersion: v1
kind: Service
metadata:
  name: discovery-service-service
spec:
  selector:
    app: discovery-service
  ports:
    - protocol: TCP
      port: 8761
      targetPort: 8761
  type: LoadBalancer

# Comando para ejecucion
# kubectl apply -f ./devops/deploy/service.yaml